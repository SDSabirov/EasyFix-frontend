# nuxt-og-image's optional peers (sharp ^0.34, tailwindcss ^4) conflict with our
# sharp 0.35 devDep and tailwindcss 3. They are unused at runtime (ogImage module
# is disabled in nuxt.config), so relax peer resolution — required for
# `npm clean-install` on Cloudflare Pages CI.
legacy-peer-deps=true
